[Unit]
Description=Aham Web (aham.ro)
After=network.target

[Install]
WantedBy=default.target

[Service]
WorkingDirectory=/usr/local/aham
ExecStart=make start-web
ExecReload=/bin/kill -s HUP $MAINPID
ExecStop=/bin/kill -s QUIT $MAINPID